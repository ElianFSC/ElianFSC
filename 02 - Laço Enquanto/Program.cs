// Variavel de indice
int indice = 1;

// Laço de Repetição 
while (indice < 11) // ou (indice <= 10)
{
    Console.WriteLine(indice);
    // indice++; (++ vai adicionando de 1 em 1)
    indice += 2; //+= vai adionionando conforme defenido
}