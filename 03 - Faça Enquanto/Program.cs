// Faça Enquanto
int indice = 10;

// Laço
do
{
    Console.WriteLine(indice);
    // indice--; ( -- vai retirando de 1 em 1
    indice -= 2; // -= retira conforme definido
} while (indice > 0);